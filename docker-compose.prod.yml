# Producción (spaceteam.uma.es): todo en Docker. El TLS lo termina el proxy de la UMA,
# que reenvía por HTTP al puerto 80 de este servidor.
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
# Variables en .env (no se sube a git): ver .env.example. Lo usa deploy_prod.sh.
services:
  frontend:
    ports: !override
      - "${HTTP_PORT:-80}:80"
    volumes:
      - ./nginx.prod.conf:/etc/nginx/nginx.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/nginx-health"]
      interval: 30s
      timeout: 3s
      retries: 3

  inventory-backend:
    # El backend solo se alcanza a través de nginx (/api)
    ports: !override
      - "127.0.0.1:3001:3001"
    environment:
      - JWT_SECRET=${JWT_SECRET:?define JWT_SECRET en .env}
      - FRONTEND_URL=https://spaceteam.uma.es
